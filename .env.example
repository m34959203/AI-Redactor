# AI-Redactor Environment Variables
# Copy this file to .env and fill in your values

# ============ DATABASE ============
# PostgreSQL connection (required for persistent storage)
# Use DATABASE_URL for cloud platforms (Railway, Heroku, Render)
DATABASE_URL=postgresql://user:password@localhost:5432/ai_redactor

# Or use individual variables for local development:
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=ai_redactor
# DB_USER=postgres
# DB_PASSWORD=your_password

# SSL mode for database (set to 'false' for local development)
# DB_SSL=true

# Enable SQL query logging (for debugging)
# DB_LOG=true

# ============ AI API ============
# Two providers supported: Groq (primary, faster) and OpenRouter (fallback)
# Configure at least one. If both are set, Groq is used first.

# Groq API Key (recommended - faster, 30 req/min free)
# Get free key at https://console.groq.com/keys
GROQ_API_KEY=gsk_your-groq-key-here

# OpenRouter API Key (fallback - 200 req/day free)
# Get free key at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Application URL (used for OpenRouter referrer header)
APP_URL=https://ai-redactor.railway.app

# ============ SERVER ============
# Server port (default: 3001)
PORT=3001

# Node environment (development/production)
NODE_ENV=production

# ============ CLIENT-SIDE ============
# API URL (optional, auto-detected in production)
# Only needed for local development when running frontend separately
# VITE_API_URL=http://localhost:3001

# ============ SOCIAL MEDIA - INSTAGRAM ============
# Meta/Facebook App credentials (required for Instagram integration)
# Get these from https://developers.facebook.com/apps/

# Facebook App ID
FACEBOOK_APP_ID=your_app_id_here

# Facebook App Secret (keep this secure!)
FACEBOOK_APP_SECRET=your_app_secret_here

# Webhook verification token (choose any secure random string)
# This is used to verify webhook setup from Meta Developer Console
INSTAGRAM_WEBHOOK_VERIFY_TOKEN=your_random_verify_token_here

# ============ SOCIAL MEDIA - TELEGRAM (future) ============
# TELEGRAM_BOT_TOKEN=your_bot_token_here
# TELEGRAM_CHAT_ID=your_chat_id_here
