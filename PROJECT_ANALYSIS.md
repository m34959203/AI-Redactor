# Комплексный анализ проекта AI-Redactor на соответствие ТЗ

**Дата анализа:** 20 декабря 2025 г.
**Версия:** 2.0 (с учётом критических замечаний)

---

## ⚠️ КРИТИЧЕСКИЕ НЕСООТВЕТСТВИЯ ТЗ

Выявлены **3 критические проблемы**, блокирующие приёмку проекта:

| # | Проблема | Статус | Влияние |
|---|----------|--------|---------|
| 1 | **Нумерация страниц** не отображается в итоговом PDF | ❌ Не работает | Критично |
| 2 | **Содержание** отсутствует или размещено в конце документа | ❌ Не соответствует | Критично |
| 3 | **Сортировка по тематическим разделам** не реализована | ❌ Отсутствует | Критично |

### Требуемые разделы (из практики журнала):
- ТЕХНИЧЕСКИЕ НАУКИ
- ПЕДАГОГИЧЕСКИЕ НАУКИ
- ЕСТЕСТВЕННЫЕ И ЭКОНОМИЧЕСКИЕ НАУКИ
- *(пустые разделы не выводить)*

---

## Резюме

| Роль | Оценка | Статус |
|------|--------|--------|
| Product Owner | **55%** | ❌ Критические функции отсутствуют |
| Prompt Engineer | 90% | ✅ Качественные промпты |
| UX/UI Designer | 70% | ⚠️ Нет UI для разделов |
| Full Stack Developer | 65% | ⚠️ Неполная реализация PDF |

**Общая оценка: 70%** — Проект **НЕ соответствует** критериям приёмки P0.

---

## 1. Анализ с точки зрения Product Owner

### ❌ Критические проблемы (блокеры)

#### 1.1 Нумерация страниц

**Требование ТЗ:** "Автоматически нумеровать страницы всего выпуска"

**Текущая реализация:**
```javascript
// pdfGenerator.js:107-117
const addPageNumber = (doc, pageNum) => {
  doc.setFontSize(10);
  doc.text(String(pageNum), PAGE_WIDTH / 2, PAGE_HEIGHT - 10, { align: 'center' });
};
```

**Проблема:**
- Функция существует, но вызывается не на всех страницах
- При серверной генерации (LibreOffice + pdftk/pdfunite) нумерация **полностью отсутствует**
- Сервер просто склеивает PDF-файлы без добавления номеров страниц

**Файл:** `server/index.js:126-152` — функция `mergePdfs()` не добавляет нумерацию

#### 1.2 Содержание (Table of Contents)

**Требование ТЗ:** "объединять титульный лист, вступительные страницы, статьи, **содержание** и заключение"

**Текущая реализация:**
```javascript
// pdfGenerator.js:871-874
// 4. Table of Contents (inserted after description, we need to note the page)
const tocStartPage = currentPage + 1;
generateTableOfContents(doc, tocEntries, tocStartPage);
```

**Проблемы:**
1. Содержание генерируется **после статей**, а не перед ними
2. В серверной генерации содержание **полностью отсутствует**
3. Нет группировки по тематическим разделам

#### 1.3 Сортировка по тематическим разделам

**Требование:** Статьи должны группироваться по разделам:
- ТЕХНИЧЕСКИЕ НАУКИ
- ПЕДАГОГИЧЕСКИЕ НАУКИ
- ЕСТЕСТВЕННЫЕ И ЭКОНОМИЧЕСКИЕ НАУКИ

**Текущая реализация:** ❌ **ОТСУТСТВУЕТ**

```javascript
// languageDetection.js - сортировка только по языку автора
export const sortArticlesByLanguage = (articles) => {
  return [...articles].sort((a, b) => {
    const priorityA = getLanguagePriority(a.language);
    const priorityB = getLanguagePriority(b.language);
    // ...только по языку, НЕ по тематике
  });
};
```

**Что нужно реализовать:**
1. AI-определение тематического раздела статьи
2. Группировка статей по разделам
3. Добавление заголовков разделов в PDF
4. Пропуск пустых разделов (без статей)

### ⚠️ Частично выполненные требования

| # | Требование ТЗ | Статус | Проблема |
|---|---------------|--------|----------|
| 1 | Сортировка по автору | ⚠️ | Порядок: Кириллица→Латиница (в ТЗ наоборот) |
| 2 | 4 пустые строки перед статьёй | ⚠️ | Только клиентская генерация |

### ✅ Выполненные требования

| # | Требование ТЗ | Статус | Реализация |
|---|---------------|--------|------------|
| 1 | Загрузка .docx | ✅ | `fileValidation.js` |
| 2 | AI извлечение названия и автора | ✅ | `aiApi.js:extractMetadataWithAI()` |
| 3 | Ручное редактирование | ✅ | `ArticlesList.jsx` |
| 4 | Скачивание PDF | ✅ | `downloadPDF()` |
| 5 | Проверка орфографии | ✅ | `checkSpelling()` |
| 6 | Модуль рецензии | ✅ | `reviewArticle()` |
| 7 | Архив выпусков | ✅ | IndexedDB |

### Итоговая оценка P0 (критерии приёмки)

| # | Критерий | Статус |
|---|----------|--------|
| 1 | Работает загрузка .docx | ✅ |
| 2 | AI корректно извлекает название и автора | ✅ |
| 3 | Автоматическая сортировка: латиница → кириллица | ❌ Инвертировано |
| 4 | Генерируется единый PDF с правильным содержанием | ❌ Содержание в конце/отсутствует |
| 5 | Перед каждой статьёй ровно 4 пустые строки | ⚠️ Только клиент |
| 6 | Сквозная нумерация страниц по всему выпуску | ❌ Не работает |
| 7 | Готовый PDF скачивается | ✅ |
| 8 | Работает ручное редактирование названия и автора | ✅ |
| 9 | Работает проверка орфографии | ✅ |
| 10 | Модуль рецензии анализирует статью | ✅ |

**Выполнено: 6 из 10** (требуется 10 из 10)

---

## 2. Анализ с точки зрения Prompt Engineer

### Оценка: 90% ✅

Промпты качественные, но **отсутствует промпт для определения тематического раздела**.

#### Необходимо добавить:

```javascript
// Предлагаемый промпт для определения раздела
export const detectArticleSection = async (content, title) => {
  const prompt = `Определи тематический раздел научной статьи.

ДОСТУПНЫЕ РАЗДЕЛЫ:
1. ТЕХНИЧЕСКИЕ НАУКИ - инженерия, IT, машиностроение, строительство
2. ПЕДАГОГИЧЕСКИЕ НАУКИ - образование, методика преподавания, дидактика
3. ЕСТЕСТВЕННЫЕ И ЭКОНОМИЧЕСКИЕ НАУКИ - физика, химия, биология, экономика, финансы

ПРАВИЛА:
- Выбери ОДИН наиболее подходящий раздел
- Основывайся на ключевых словах и теме статьи
- Если статья междисциплинарная, выбери преобладающую тему

НАЗВАНИЕ СТАТЬИ: "${title}"
НАЧАЛО ТЕКСТА: ${content.substring(0, 1500)}

Ответь JSON: {"section": "ТЕХНИЧЕСКИЕ НАУКИ" | "ПЕДАГОГИЧЕСКИЕ НАУКИ" | "ЕСТЕСТВЕННЫЕ И ЭКОНОМИЧЕСКИЕ НАУКИ"}`;

  // ...
};
```

### Существующие промпты (хорошие)

| Промпт | Оценка | Комментарий |
|--------|--------|-------------|
| `extractMetadataWithAI` | ⭐⭐⭐⭐⭐ | Отличные примеры для RU/EN/KZ |
| `checkSpelling` | ⭐⭐⭐⭐⭐ | Правильная фильтрация терминов |
| `reviewArticle` | ⭐⭐⭐⭐⭐ | 5 критериев с описанием шкалы |
| `detectArticleSection` | ❌ | **Отсутствует** |

---

## 3. Анализ с точки зрения UX/UI Designer

### Оценка: 70% ⚠️

#### Отсутствующие UI-элементы

| Элемент | Статус | Влияние |
|---------|--------|---------|
| Выбор/отображение раздела статьи | ❌ | Критично |
| Группировка статей по разделам | ❌ | Критично |
| Предпросмотр содержания | ❌ | Средне |
| Индикатор позиции содержания | ❌ | Низко |

#### Необходимо добавить в UI:

1. **Dropdown для выбора раздела** в карточке статьи
2. **Визуальная группировка** статей по разделам (с заголовками)
3. **Drag-and-drop** между разделами
4. **Предпросмотр структуры** журнала перед генерацией

#### Позитивные решения (сохранить):

- ✅ Drag & Drop загрузка
- ✅ Inline редактирование
- ✅ Прогресс-индикатор
- ✅ Toast-уведомления

---

## 4. Анализ с точки зрения Full Stack Developer

### Оценка: 65% ⚠️

#### Архитектурные проблемы

##### 4.1 Клиентская генерация PDF (`pdfGenerator.js`)

```javascript
// Проблема: содержание генерируется В КОНЦЕ
// Строка 871-874
const tocStartPage = currentPage + 1;
generateTableOfContents(doc, tocEntries, tocStartPage);
```

**Нужно:**
1. Сначала собрать все статьи и их страницы
2. Сгенерировать содержание
3. Вставить содержание ПЕРЕД статьями
4. Пересчитать номера страниц

##### 4.2 Серверная генерация PDF (`server/index.js`)

```javascript
// Проблема: просто склейка PDF без логики
async function mergePdfs(pdfPaths, outputPath) {
  await execAsync(`pdftk ${inputFiles} cat output "${outputPath}"`);
}
```

**Проблемы:**
- ❌ Нет генерации содержания
- ❌ Нет нумерации страниц
- ❌ Нет заголовков разделов
- ❌ Нет пустых строк перед статьями

**Решение:** Использовать библиотеку `pdf-lib` для полного контроля над PDF.

##### 4.3 Отсутствует модель данных для разделов

```javascript
// Текущая структура статьи
const article = {
  id, file, title, author, language, content
};

// Необходимо добавить
const article = {
  id, file, title, author, language, content,
  section: 'ТЕХНИЧЕСКИЕ НАУКИ' | 'ПЕДАГОГИЧЕСКИЕ НАУКИ' | 'ЕСТЕСТВЕННЫЕ И ЭКОНОМИЧЕСКИЕ НАУКИ'
};
```

#### План исправления

| # | Задача | Приоритет | Сложность |
|---|--------|-----------|-----------|
| 1 | Добавить AI-определение раздела | P0 | Средняя |
| 2 | Добавить поле `section` в модель статьи | P0 | Низкая |
| 3 | UI для отображения/редактирования разделов | P0 | Средняя |
| 4 | Генерация содержания В НАЧАЛЕ PDF | P0 | Высокая |
| 5 | Нумерация страниц (сервер) | P0 | Высокая |
| 6 | Заголовки разделов в PDF | P0 | Средняя |
| 7 | Пропуск пустых разделов | P0 | Низкая |

---

## 5. Сводная таблица соответствия ТЗ

### Функциональные требования

| # | Требование | Статус | Комментарий |
|---|------------|--------|-------------|
| 1 | Drag & drop загрузка .docx | ✅ | Работает |
| 2 | Загрузка титульного листа | ✅ | Работает |
| 3 | AI-извлечение метаданных | ✅ | Работает |
| 4 | Ручное редактирование | ✅ | Работает |
| 5 | Сортировка по разделам | ❌ | **Не реализовано** |
| 6 | Сортировка по автору внутри раздела | ⚠️ | Порядок инвертирован |
| 7 | Генерация содержания | ❌ | В неправильном месте |
| 8 | Нумерация страниц | ❌ | **Не работает** |
| 9 | Скачивание PDF | ✅ | Работает |
| 10 | Проверка орфографии | ✅ | Работает |
| 11 | Модуль рецензии | ✅ | Работает |
| 12 | Архив выпусков | ✅ | Работает |

### Критерии приёмки P0

| # | Критерий | Статус |
|---|----------|--------|
| 1 | Загрузка .docx | ✅ PASS |
| 2 | AI извлечение | ✅ PASS |
| 3 | Сортировка латиница→кириллица | ❌ FAIL |
| 4 | PDF с содержанием | ❌ FAIL |
| 5 | 4 пустые строки | ⚠️ PARTIAL |
| 6 | Нумерация страниц | ❌ FAIL |
| 7 | Скачивание PDF | ✅ PASS |
| 8 | Редактирование | ✅ PASS |
| 9 | Проверка орфографии | ✅ PASS |
| 10 | Модуль рецензии | ✅ PASS |

**Результат: 6/10 PASS — Проект НЕ принят**

---

## 6. Рекомендации по исправлению

### 🔴 Критический приоритет (P0)

1. **Реализовать сортировку по разделам:**
   - Добавить промпт `detectArticleSection`
   - Добавить поле `section` в модель
   - Группировать статьи по разделам в UI и PDF

2. **Исправить генерацию содержания:**
   - Переместить содержание в начало (после описания)
   - Добавить заголовки разделов
   - Пропускать пустые разделы

3. **Исправить нумерацию страниц:**
   - Для серверной генерации: использовать `pdf-lib`
   - Добавить сквозную нумерацию

### 🟡 Высокий приоритет (P1)

4. Исправить порядок сортировки внутри раздела (латиница→кириллица)
5. Добавить UI для управления разделами
6. Добавить тесты для PDF генерации

### 🟢 Средний приоритет (P2)

7. Предпросмотр PDF перед скачиванием
8. Drag-and-drop перемещение между разделами
9. Поддержка кастомных названий разделов

---

## 7. Заключение

### Статус проекта: ❌ НЕ ГОТОВ К ПРИЁМКЕ

Проект требует доработки критических функций:

1. ❌ **Группировка по тематическим разделам** — отсутствует полностью
2. ❌ **Содержание** — генерируется в неправильном месте
3. ❌ **Нумерация страниц** — не работает при серверной генерации

### Что работает хорошо:

- ✅ AI-извлечение метаданных (90% качества)
- ✅ Проверка орфографии
- ✅ Модуль рецензирования
- ✅ Архив выпусков
- ✅ UX загрузки и редактирования

### Оценка трудозатрат на исправление:

| Задача | Оценка |
|--------|--------|
| AI-определение разделов | 4-8 часов |
| UI для разделов | 8-16 часов |
| Исправление содержания | 8-16 часов |
| Нумерация страниц (pdf-lib) | 16-24 часа |
| Тестирование | 8-16 часов |
| **ИТОГО** | **44-80 часов** |

---

*Анализ выполнен на основе исходного кода проекта и требований ТЗ.*
*Версия 2.0 — с учётом критических замечаний заказчика.*
