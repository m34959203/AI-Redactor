# 🚂 Деплой на Railway.com

Пошаговое руководство по деплою AI-Редактора на Railway.

## 📋 Требования

- Аккаунт на [Railway.com](https://railway.app/)
- GitHub репозиторий с проектом
- API ключ OpenRouter (уже есть в .env.example)

## 🚀 Быстрый деплой

### Способ 1: Через Railway CLI (рекомендуется)

1. **Установите Railway CLI:**
```bash
npm i -g @railway/cli
```

2. **Войдите в аккаунт:**
```bash
railway login
```

3. **Инициализируйте проект:**
```bash
railway init
```

4. **Добавьте переменную окружения:**
```bash
railway variables set VITE_OPENROUTER_API_KEY=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
```

5. **Деплой:**
```bash
railway up
```

6. **Откройте приложение:**
```bash
railway open
```

### Способ 2: Через GitHub интеграцию

1. **Подключите GitHub:**
   - Откройте [railway.app/new](https://railway.app/new)
   - Выберите "Deploy from GitHub repo"
   - Авторизуйте доступ к GitHub
   - Выберите репозиторий `AI-Redactor`

2. **Настройте переменные окружения:**
   - Перейдите в настройки проекта
   - Вкладка "Variables"
   - Добавьте переменную:
     - **Key:** `VITE_OPENROUTER_API_KEY`
     - **Value:** `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`

3. **Деплой:**
   - Railway автоматически задеплоит проект
   - Следите за процессом в разделе "Deployments"

4. **Получите URL:**
   - Перейдите в "Settings" → "Domains"
   - Скопируйте сгенерированный URL или добавьте кастомный домен

## ⚙️ Конфигурация

Railway автоматически определяет Node.js проекты и использует следующую конфигурацию:

### Build команда:
```bash
npm install && npm run build
```

### Start команда:
```bash
npm run preview -- --host 0.0.0.0 --port $PORT
```

## 🔧 Настройка проекта

### 1. Vite Preview Server

Обновите `vite.config.js` для поддержки Railway:

```javascript
export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    open: true
  },
  preview: {
    host: '0.0.0.0',
    port: process.env.PORT || 4173
  },
  // ... остальная конфигурация
});
```

### 2. Package.json scripts

Убедитесь, что в `package.json` есть скрипты:

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}
```

## 🌍 Переменные окружения

Railway автоматически предоставляет:
- `PORT` - порт для приложения
- `RAILWAY_ENVIRONMENT` - окружение (production/staging)
- `RAILWAY_PROJECT_ID` - ID проекта
- `RAILWAY_SERVICE_ID` - ID сервиса

Вам нужно добавить только:
- `VITE_OPENROUTER_API_KEY` - ключ API OpenRouter

## 📊 Мониторинг

1. **Логи:**
```bash
railway logs
```

2. **Метрики:**
   - Откройте проект в Railway Dashboard
   - Вкладка "Metrics" показывает использование CPU, RAM, сети

3. **Health Check:**
   Railway автоматически проверяет доступность приложения

## 🔄 Автоматический деплой

Railway автоматически деплоит при:
- Push в main/master ветку (если настроена GitHub интеграция)
- Ручной команде `railway up`

Настройка автоматического деплоя:
1. Settings → "Deploy Triggers"
2. Выберите ветку для автодеплоя
3. Включите "Auto Deploy"

## 💰 Стоимость

Railway предоставляет:
- **$5 бесплатных кредитов** каждый месяц
- **500 часов** выполнения (на Starter Plan)
- Приложение засыпает после 15 минут бездействия на бесплатном плане

Для production используйте Hobby Plan ($5/месяц).

## 🐛 Troubleshooting

### Проблема: Build не проходит

**Решение:**
```bash
# Очистите кеш Railway
railway run npm ci
railway run npm run build
```

### Проблема: Приложение не запускается

**Решение:** Проверьте логи:
```bash
railway logs
```

Убедитесь, что `preview` скрипт использует правильный порт:
```bash
vite preview --host 0.0.0.0 --port $PORT
```

### Проблема: API ключ не работает

**Решение:**
1. Проверьте наличие переменной:
```bash
railway variables
```

2. Пересоздайте переменную:
```bash
railway variables set VITE_OPENROUTER_API_KEY=your_key_here
```

3. Перезапустите деплой:
```bash
railway up --detach
```

### Проблема: 502 Bad Gateway

**Решение:**
- Увеличьте timeout в настройках проекта
- Проверьте, что приложение слушает правильный порт
- Убедитесь, что build завершился успешно

## 🔐 Безопасность

1. **Не коммитьте .env файлы:**
   - `.env` уже в `.gitignore`
   - Используйте Railway variables для секретов

2. **Используйте Railway Secrets:**
```bash
railway variables set SECRET_KEY=your_secret
```

3. **Настройте CORS** (если нужно):
   - Railway предоставляет HTTPS автоматически
   - Настройте allowed origins в коде

## 📈 Масштабирование

Railway поддерживает:
- **Вертикальное масштабирование:** увеличение CPU/RAM
- **Горизонтальное масштабирование:** запуск нескольких реплик (платно)

Настройка в Railway Dashboard:
1. Settings → "Resources"
2. Выберите план (Starter/Hobby/Pro)
3. Настройте количество реплик

## 🔗 Полезные команды

```bash
# Просмотр статуса
railway status

# Просмотр переменных
railway variables

# Открыть в браузере
railway open

# Подключиться к shell
railway shell

# Просмотр использования
railway usage

# Откат к предыдущему деплою
railway rollback
```

## 📚 Дополнительные ресурсы

- [Railway Documentation](https://docs.railway.app/)
- [Railway CLI Reference](https://docs.railway.app/develop/cli)
- [Community Discord](https://discord.gg/railway)
- [Status Page](https://status.railway.app/)

## 💡 Советы

1. **Используйте GitHub интеграцию** для автоматического деплоя
2. **Настройте уведомления** в Railway Discord webhook
3. **Мониторьте использование** чтобы не превысить лимиты
4. **Используйте Environment Variables** для разных окружений
5. **Добавьте Health Check** для мониторинга

---

**Production URL:** [https://airesume.zhezu.kz](https://airesume.zhezu.kz) — субдомен Жезказганского университета имени О.А. Байконурова

Готово! Ваше приложение задеплоено и доступно на домене университета 🎉
